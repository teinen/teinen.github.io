import Vue from 'vue'
import AboutSection from "@/sections/AboutSection.vue"

describe('AboutSection', () => {

  test('has components', () => {
    expect(typeof AboutSection.components).toBe('object')
  })
})